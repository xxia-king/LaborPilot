# AI 工作台账

## 2026-08-27｜neat-freak 发布候选收尾（待人工复核）

- 2026-08-27｜Codex｜完成 LaborPilot 1.3.0 文档、任务痕迹、版本和独立公开包对齐审查，未 commit、未 push｜session：`2026-08-27-laborpilot-1.3.0-neat-freak`｜AI 生成，待人工复核｜详见 `.agent/runs/2026-08-27-laborpilot-1.3.0-neat-freak.md`。

## 2026-08-26｜LaborPilot 1.3.0 发布候选（未发布）

- 本轮公开候选包版本已统一为 `1.3.0`，本地 Python 3.11／3.12 均通过 70 项测试，领域与法律版本评测 18／18 通过，版本同步、Plugin 清单、JSON 解析和 diff 检查通过。
- 独立干净公开候选包通过 Python 3.12 全量测试、版本一致性与 Plugin 校验；包内不含私有 `scripts/build_cards.py`、字节码、Finder 缓存或本地配置。
- GitHub 当前公开版本仍为 `1.2.0`；本记录对应未 commit、未 push 的 `1.3.0` 发布候选。

## 2026-08-25｜neat-freak 收尾（未发布）

- Codex 同步 Python 3.11+、仓库根目录和 CLI 参数文档，将 CI 改为 Python 3.11／3.12 matrix，补齐本地敏感配置忽略规则并清理 Finder／Python 缓存；两个 Python 版本在本地及隔离公开包中均通过 70 项测试，领域评测 17／17，版本、Plugin、链接和敏感信息检查通过。详见 `.agent/runs/2026-08-25-laborpilot-neat-freak.md`。未 commit、未 push。

## 2026-08-24｜锁定文章 PRD 执行器（未发布）

- 以锁定版《2026-08-22-LaborPilot公众号文章_v2.md》为只读 PRD，本轮未修改文章。
- 增加材料接入与事实时间轴执行器：原件哈希、文字层／OCR 路由、派生文本、时间轴、事实分层和来源关系均进入可追溯状态。
- 增加请求／抗辩矩阵执行器：知识路由只生成待复核候选；正式矩阵必须包含构成要件、事实链接、对方最强观点与回应、备选路径和失败后果。
- 增加证据链与举证责任执行器：逐要件生成待复核骨架，正式链条记录证据方向、事实／材料链接、初始举证主体、控制方、责任转移、不利后果、缺口和补证行动，并反向回写争点要件与对方路径。
- 增加法源核验结果适配器：不绑定某一 MCP 或法律数据库，将外部核验结果规范为完整条文及哈希、来源 URL／检索时间、效力起止、地域、相关日期和适用理由，并反向回写争点要件与对方路径。
- 增加专业金额执行器和版本化参数包：公式由律师／Agent 选择，脚本执行 N、N＋1、2N、三类加班工资、工伤待遇、竞业限制补偿及合计；输入来源、法源、参数包哈希、算式、中间步骤、金额和风险进入可复算台账。
- 金额节点要求明确封顶适用性，存在待确认输入时不生成金额；输入字段错误、参数日期／地域错配以及金额、算式、步骤或摘要篡改均被独立校验拦截。金额专项 8 项、当前全量 40 项本地测试及隔离公开包 40 项测试均已通过。
- 增加实质性对抗验证器：逐争点生成对方最强论证、回应、失败边界、备选路径、事实分层和引用快照；报告绑定业务状态摘要，登记及节点转换时重算并核对报告 SHA-256。专项 4 项、本地全量 44 项及隔离公开包 44 项测试均已通过。
- 增加虚构案件真实端到端回归，并补齐 `record-decision`、文书内部工作目录和 Pandoc 严格失败模式。本地及隔离公开包均为 45 项测试通过；端到端链到达 `stage_close`，真实 DOCX XML 包含虚构当事人和 70,000.00 元请求。该结果不等同于 JLS 最终 Word 版式已完成。
- 增加正式产物端到端追溯契约和执行门禁：产物登记保存交付层级、业务来源摘要、上游产物、生成器、LaborPilot 版本和文件哈希；验证保存被验证文件快照；律师审批绑定具体产物与验证报告；`trace-artifact`、阶段结案门禁和独立验证器均可复算。来源记录、上游文件、正式文件、验证报告、审批缺项和循环引用已有聚焦反例覆盖；16 项相关回归、本地全量 48 项及隔离公开包 48 项测试均通过。
- 增加知识构建统计清单和公开复算脚本：从编译载荷复算 96 张知识卡、82 张争点卡、14 张案由门卡、80 张含浙江口径卡、22 项细分案由及 1 个劳动合同纠纷兜底运行路由；149 份来源以编号、范围、大小和哈希承诺登记，不公开标题、路径或正文。
- 知识统计专项 3 项、本地全量 51 项及隔离公开包 51 项测试均通过；私有来源目录复算 149 份承诺一致，公开包完成版本、Plugin、9 个 JSON、Python 语法、敏感模式和私有构建脚本排除检查。
- 增加 JLS Word 版式修正和独立校验：正式 DOCX 保留 `仿宋_GB2312`，并确定性校验中英文字体、字号、固定 25 磅行距、缩进、标题、页面、表格与落款。初稿、最终提交版和内部 Markdown 分目录保存，生成器拒绝覆盖旧版本且只生成 `--types` 明确指定的文书。
- 最终提交版必须记录律师提交决定，直接派生自当前哈希已获批准的初稿，并作为新文件重新完成业务验证和律师审批；行动清单、内部占位符和待确认标记被阻断。专项结构测试现为 6 项通过，三类虚构 DOCX 使用独立 Fontconfig 视觉 QA 映射渲染为单页，未发现方框、裁切、重叠或落款换行；正式 OOXML 字体声明未使用回退字体替代。
- Word 交付能力最终验收：本地全量 57 项、隔离公开包全量 57 项均通过；隔离包中 30 个公开 Python 文件语法、版本 1.2.0、Plugin 清单和 10 个 JSON 均通过。隔离包共 83 个文件、1208320 字节，不含 `.git`、缓存、字节码或私有 `scripts/build_cards.py`；三类虚构 DOCX 在隔离包中实际生成、结构校验并渲染为 3 份单页 PDF，中文文本可提取。
- 建立版本化领域评测机制：领域数据集包含单一、复合、否定语境和非劳动争议场景；法律版本数据按案件相关日期选择唯一有效版本，区间重叠、空档或以公布日代替施行日都会被反例拦截。
- 评测公开边界和版本同步已加入机器门禁：数据集拒绝内部卡号／案由门字段，CLI 不输出知识卡正文；`evals/evals.json.package_version` 由 `VERSION` 统一同步和校验。
- Task 19 当前基线验收：本地全量 65 项、隔离公开包全量 65 项均通过，当前领域与法律版本结果 17／17 通过。隔离包的版本 1.2.0、知识统计、Plugin、31 个 Python 文件、11 个 JSON、敏感赋值与内部评测字段检查均通过。隔离包 `/private/tmp/laborpilot-domain-public.h7WUQv/LaborPilot` 共 84 个文件、1039765 字节，不含 `.git`、缓存、字节码或私有 `scripts/build_cards.py`。
- Task 20.1 增加逐页材料索引：每份有明确页数的材料保存连续页码、逐页文字层状态、可提取字符数和 OCR 状态；分页索引缺失或与逐材料接入记录不一致时，材料门禁阻断。聚焦回归 10 项、本地与隔离公开包全量 65 项均通过。
- Task 20.2 增加 `analyze_procedure.py`：对每个已复核争点记录并校验时效、管辖、一裁终局、临时救济和后续救济路径；所引法源、日期、地域和摘要均可复核。金额与程序是两组独立完成条件，`reviewed_procedure_path` 不可豁免。本地与隔离公开包全量 67 项、领域评测 17／17 均通过。
- Task 20.3 增加结构化事实冲突：冲突事实双向记录处理状态、说明和待核实行动；禁止自引用、不存在引用、单向关系、双方状态不一致以及将未解决冲突静默标记为 `supported`。
- 对抗验证报告升级为六项可重算检查，新增 `fact_conflicts` 和 `procedure_completeness`，并输出事实冲突矩阵和程序矩阵；`procedural_assessments[]` 纳入业务状态摘要。未解决冲突、程序整段或字段缺失、待确认程序项、摘要篡改，以及事实或程序变化后沿用旧报告均有反例覆盖。
- Task 20.3 最终验收：本地与隔离公开包全量 70 项、领域评测 17／17 均通过。隔离包 `/private/tmp/laborpilot-conflict-public.Vrkj2z/LaborPilot` 的版本 1.2.0、Plugin、全部 JSON 和 32 个公开 Python 文件语法均通过；共 85 个文件、1318912 字节，不含 `.git`、缓存、字节码、`.DS_Store` 或私有 `scripts/build_cards.py`。
- 对抗性审查修复了 OCR 派生文本可能被重新接入覆盖、事实状态变更生成重复 ID、大文件无界读取、越界派生路径、旧占位争点／证据无法受控升级，以及“全部证据待核实却标成充分”等问题。
- 金额执行器加入前的前序本地与隔离公开包均为 32 项测试通过；加入金额能力后，本地与排除 `.git`、缓存、字节码及私有 `scripts/build_cards.py` 的隔离公开包均为 40 项测试通过。版本同步、Python 编译、Plugin 校验、6 个 JSON 解析、`git diff --check` 和敏感信息模式扫描通过；隔离包确认不包含私有构建脚本。
- Task 20.4 已形成 `references/article-prd-capability-audit.md`：逐项核对数字口径、知识输出边界、工作链、1＋9 Skill、材料／事实／争点／证据／法源／金额／程序、三类 DOCX、六项对抗验证、离线与外部能力边界、通用 Agent／Codex 入口及发布状态。未发现需要继续补代码的文章能力缺口。
- Task 20.4 最终验收：本地与最终干净公开包均为 70 项测试通过、领域与法律版本评测 17／17；版本 1.2.0、知识统计、Plugin、32 个 Python 文件、11 个 JSON、敏感赋值和禁止项检查均通过。隔离包 `/private/tmp/laborpilot-final-public.VXRYP1/LaborPilot` 共 86 个文件、1129411 字节。
- 本轮 PRD 能力实现主线已完成，但仍为未发布改动；本轮未 commit、未 push。锁定文章 SHA-256 保持不变，文章所写 v1.1.0 是历史状态，只有完成当次授权的 commit／push 后才能认定本轮增强已在 GitHub 公开。

## 2026-08-22｜LaborPilot 1.2.0

- 完成 8 个领域场景回归、争点路由增强和工作流节点机器完成条件。
- 增加结构化豁免、真实初稿产物门禁及报告支撑的验证门禁。
- 对抗性审查发现并修复验证升级路径的两处问题：无报告可尝试升级、有报告升级后仍被下游错误阻断。
- 版本、Plugin 清单、全部内置 Skill、README 和 CHANGELOG 已同步为 `1.2.0`。
- 发布前验证结果：14 项单元测试通过，版本同步、Plugin 清单、JSON 解析与 diff 检查通过。
- 长期未完成事项见 `TASKS.md`，不得把目标清单表述为当前已实现能力。
