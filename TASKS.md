# LaborPilot 优化任务

本清单记录公众号文章所描述的目标能力与公开插件当前实现之间的差距。完成状态必须以代码、动态测试和干净公开包验证为准，不以文案或 Skill 指令代替实现证据。

## 当前串行顺序

1. **构建统计清单（已完成）**：已证明 README 和锁定文章使用的 149 部资料、96 张知识卡和 23 类案由口径，并通过本地与隔离公开包复核。
2. **Word 最终版式与交付分层（已完成）**：在现有真实 DOCX 内容链和产物追溯基础上，补齐 JLS 定稿规范，并明确律师复核初稿与最终提交版。
3. **领域评测持续机制（当前基线已完成）**：已建立版本化数据集和公开评测执行器，当前覆盖反例、复合争点、误召回和法律生效边界；今后每次路由或法律口径变更都应递增数据集版本，不将当前用例数量视为永久完成。
4. **材料逐页索引（已完成）**：每份有明确页数的材料均生成连续分页索引，记录逐页文字层、可提取字符数和 OCR 状态，并由材料门禁复核。
5. **时效、管辖与程序路径执行器（已完成）**：已将时效、一裁终局、临时救济和后续救济从 Skill 指令升级为逐争点可执行台账；金额与程序双门禁均不可豁免。
6. **事实冲突与程序遗漏独立验证（已完成）**：事实冲突使用双向关系、处理状态、说明和待核实行动；对抗报告以六项检查覆盖事实冲突和程序完整性，事实或程序变化后旧报告失效。
7. **锁定文章最终能力审计（已完成）**：声明—证据矩阵未发现需要继续补代码的功能缺口；本地与最终干净公开包均通过 70 项测试和 18／18 领域与法律版本评测，本轮 PRD 能力实现主线结束。文章中的 v1.1.0 作为历史状态保留；GitHub 已发布 `v1.3.0`。

在前一项完成本地全量及干净公开包验收前，不并行启动后一项。

## 已完成，待人工复核

- [x] 将 8 个劳动争议领域场景纳入自动回归，并对核心争点名称作业务断言。
- [x] 为常见案件意图增加查询扩展，保证多争点案件按已识别意图保留核心争点。
- [x] 为材料、事实、争点、证据、法源、请求、初稿和验证增加机器完成条件。
- [x] 禁止 `record-validation --status pass` 在没有 JSON 验证报告时自报通过。
- [x] 增加结构化、经用户确认的节点豁免；初稿产物和验证报告不得豁免。
- [x] 增加空状态反例与完整有效案件状态走通测试。

## 长期优化目标

### P0：公开包可复核性

- [x] 在干净公开 checkout 中完成虚构案件端到端办案回归，覆盖任务确认、材料接入、事实时间轴、争点、证据、法源、金额请求、策略确认、真实 DOCX 起草、双重验证、律师审批和阶段回写。
- [x] 生成可复核的构建统计清单，分别证明 149 部资料、96 张知识卡和 23 类案由的统计口径与当前发布包一致。
- [x] 建立领域评测的版本化持续机制，当前基线已覆盖单一争点、复合争点、否定语境、非劳动争议和法律生效边界，并通过本地与干净公开包验收。路由或法律口径变更时继续递增数据集版本。

### P1：材料与事实执行器

- [x] 实现材料接入执行器：原件哈希、文件类型与页数、逐页文字层／字符数／OCR 状态、派生文本、材料索引和可视复核状态；下游门禁复算源文件哈希和分页索引。
- [x] 实现事实时间轴执行器，持续区分有证据事实、当事人陈述、对方主张、争议事实和待核实事项；自动日期句只作为待核实候选；事实冲突以双向关系、处理状态、说明和待核实行动记录。
- [x] 实现请求／抗辩矩阵执行器：内置知识只生成待复核争点候选；正式矩阵必须包含构成要件、事实链接、对方最强观点与回应、备选路径和失败后果，引用的事实／证据／法源 ID 均须存在。

### P1：证据、法源与计算

- [x] 实现证据链和举证责任执行器，覆盖证据缺口、单位掌握证据和举证不能后果。
- [x] 建立官方来源／法律数据库适配接口，保存法规、条文、效力、地域和时间锚点，不绑定单一 MCP 服务。
- [x] 实现 N、N＋1、2N、三类加班工资、工伤待遇、竞业限制补偿及合计等专业计算器；维护可追溯的版本化参数包，并以待确认输入门禁、独立重算和干净公开包测试验收。

### P2：验证与交付

- [x] 将对抗性验证从指令要求升级为可重算的业务结果，以六项检查覆盖对方最强论证及回应、失败边界、事实分层、事实冲突、引用一致性和程序完整性，并以状态摘要、报告哈希及干净公开包测试防止伪造或过期报告。
- [x] 使 Word 产物符合 JLS 定稿规范，并明确区分内部 Markdown、律师复核初稿和最终提交版目录。
- [x] 为所有正式产物补齐来源链、版本、哈希、验证报告和律师审批的端到端追溯。

## 2026-08-25 收尾审查

- [x] **[review] 文档、命令与发布候选包同步审查**：统一 Python 3.11+ 和仓库根目录执行口径，修正专业 Skill 中的 CLI 必填参数；本地及最新隔离公开包均通过 70 项测试、17／17 领域与法律版本评测、版本同步和 Plugin 校验。证据见 [运行记录](.agent/runs/2026-08-25-laborpilot-neat-freak.md)。
- [x] **[review] CI Python 版本**：`.github/workflows/version-check.yml` 已由 Python 3.9 改为 3.11／3.12 matrix；两个版本在本地及最新隔离公开包中均通过版本同步和 70 项测试。
- [x] **[review] 本地敏感配置忽略规则**：`.gitignore` 已加入 `.env`、`.env.*`、`config.local.json` 和 `config.*.local.json`，并显式保留 `.env.example`。
- [x] **[review] 忽略缓存清理**：已删除 7 个 `.DS_Store`、2 个 `__pycache__` 目录及其中 28 个 `.pyc`；清理后复查为 0。

## 2026-08-27 发布复核

- [x] **[review] 1.3.0 发布收口**：版本、双 Python 全量测试、18／18 领域与法律版本评测、Plugin、干净公开包及私有构建脚本排除检查均通过；GitHub Runner 补齐 Pandoc 后，双 Python CI 与自动发布工作流通过，`v1.3.0` tag 和 GitHub Release 已发布。证据见 [运行记录](.agent/runs/2026-08-27-laborpilot-1.3.0-neat-freak.md)。
